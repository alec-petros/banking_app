<%= form_for @bank do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <br>
  <%= f.label :city %>
  <%= f.collection_select :city_id, City.all, :id, :name, {:include_blank => "Create New City"} %>
  <br>
  <%= f.submit %>
<% end %>
